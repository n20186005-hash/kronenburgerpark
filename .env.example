# Site domain used for canonical URLs, hreflang, Open Graph and JSON-LD.
# Set this to the deployed domain of the current site. When unset, it defaults
# to "kronenburgerpark.com" so local builds keep working.
CURRENT_SITE_DOMAIN=kronenburgerpark.com
